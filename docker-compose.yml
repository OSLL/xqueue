version: "2.1"

services:
  xqueue:
    container_name: "xqueue"
    #build: ./
    image: 451687910923/xqueue-custom:latest
    volumes:
      - .:/edx/app/xqueue
    networks:
      default:
        aliases:
          - edx.devstack.xqueue
    ports:
      - 18040:18040
